/*
 * PostCSS Configuration for Tailwind CSS v4
 * Used by Webpack to process CSS with Tailwind
 */

module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
    // Note: autoprefixer not needed (Chrome-only extension)
  },
};
